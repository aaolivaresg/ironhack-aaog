name: Automate github action

on:
  pull_request:
    branches:
      - main
      - develop

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
     
      - name: Testing simulation 
        run: echo "Testing simulation "
      - name: Ensure success 
        run: exit 0  # 
